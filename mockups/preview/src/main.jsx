import React from 'react';
import ReactDOM from 'react-dom/client';
import ProtocolPDFPreview from '../../inField-Protocol-PDF-v2.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <ProtocolPDFPreview />
  </React.StrictMode>
);
