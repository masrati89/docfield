# 📄 OUTPUT_TEMPLATE.md — תבנית קובץ הוראות

> **מטרה:** כל פלט של ה-Agent מיוצר לפי התבנית הזו.
> קובץ הוראות = מה שמריצים ב-Claude Code ישירות.

---

## מבנה קובץ TASK_XXX.md

```markdown
# TASK\_[NUMBER] — [שם המשימה]

> **עדיפות:** P0 / P1 / P2 / P3
> **זמן משוער:** X שעות
> **תלויות:** [TASK שצריך להיות מוכן קודם, אם יש]
> **מסמכי reference:** [FLOW_SPEC §X, ARCHITECTURE §X, וכו']

---

## 🎯 מטרה

[משפט אחד — מה הTask הזה משיג]

---

## 📂 קבצים שצריך לקרוא לפני

- [ ] `[path/to/file]` — [למה]
- [ ] `[path/to/mockup]` — [למה]

---

## 📂 קבצים שיווצרו / ישתנו

| פעולה  | קובץ                                | הערה        |
| ------ | ----------------------------------- | ----------- |
| CREATE | `apps/mobile/app/[path].tsx`        | [תיאור קצר] |
| MODIFY | `apps/mobile/components/[name].tsx` | [מה משתנה]  |
| CREATE | `packages/shared/types/[name].ts`   | [תיאור קצר] |

---

## 📋 הוראות ביצוע

### שלב 1 — [שם השלב]

[הוראה מדויקת מה לעשות]

**דוגמה:**
\`\`\`typescript
// קוד לדוגמה אם רלוונטי
\`\`\`

**⚠️ שים לב:**

- [אזהרה ספציפית אם יש]

---

### שלב 2 — [שם השלב]

[הוראה מדויקת]

---

## ✅ Checklist אימות לפני סיום

**TypeScript:**

- [ ] `npm run typecheck` עובר ללא שגיאות
- [ ] אין `any` — strict mode

**Design:**

- [ ] RTL + Rubik font
- [ ] רקע cr50 (#FEFDFB) — לא לבן
- [ ] fontSize ≥ 16 בכל input
- [ ] theme.colors בלבד (לא hardcoded hex)
- [ ] theme.spacing בלבד (לא magic numbers)
- [ ] Skeleton loading (לא spinner)
- [ ] Empty state אם יש רשימה

**Logic:**

- [ ] organization_id על כל query
- [ ] Iron Rules נשמרות
- [ ] Flow תואם FLOW_SPEC.md

**Build:**

- [ ] `npx turbo build` עובר
- [ ] אין console.log

---

## 🔴 Iron Rules — רלוונטיות לTask הזה

[ציין רק את הכללים שרלוונטיים לTask הספציפי הזה]

---

## ⏭️ הTask הבא אחרי זה

`TASK_[NEXT_NUMBER] — [שם]`

---

_TASK_[NUMBER].md — Generated by inField PM Agent\_
```

---

## דוגמה — TASK_001.md (מלא)

```markdown
# TASK_001 — Home Screen — מימוש מלא מ-Mockup

> **עדיפות:** P0
> **זמן משוער:** 3-4 שעות
> **תלויות:** אין (תשתית מוכנה)
> **מסמכי reference:** FLOW_SPEC §10, ARCHITECTURE §17, SCREEN_STANDARDS.md

---

## 🎯 מטרה

להחליף את `apps/mobile/app/(app)/index.tsx` הבסיסי במימוש מלא
לפי המוקאפ `inField-HomeScreen-v6-rtl.jsx`.

---

## 📂 קבצים שצריך לקרוא לפני

- [ ] `apps/mobile/mockups/inField-HomeScreen-v6-rtl.jsx` — המוקאפ הסופי
- [ ] `constants/theme.ts` — tokens
- [ ] `FLOW_SPEC.md §10` — Home Dashboard rules

---

## 📂 קבצים שיווצרו / ישתנו

| פעולה  | קובץ                                              | הערה                |
| ------ | ------------------------------------------------- | ------------------- |
| MODIFY | `apps/mobile/app/(app)/index.tsx`                 | מימוש מלא           |
| CREATE | `apps/mobile/components/home/StatsStrip.tsx`      | 2 stat cards        |
| CREATE | `apps/mobile/components/home/RecentReportRow.tsx` | שורת דוח ב-Home     |
| CREATE | `apps/mobile/components/home/ProjectCard.tsx`     | כרטיס פרויקט ב-Home |

---

## 📋 הוראות ביצוע

### שלב 1 — קרא את המוקאפ

פתח `apps/mobile/mockups/inField-HomeScreen-v6-rtl.jsx` וזהה:

- Stats Strip: 2 פריטים בלבד (טיוטות + הושלמו)
- "דוחות אחרונים": 5 דוחות לפי updated_at
- "הפרויקטים שלי": 3 פרויקטים לפי updated_at
- FAB "בדיקה חדשה" — תחתית מרכז

### שלב 2 — Data Layer

בצע queries ב-React Query:

- `useQuery(['reports', 'recent'])` → 5 דוחות אחרונים
- `useQuery(['projects', 'recent'])` → 3 פרויקטים אחרונים
- `useQuery(['stats'])` → count draft+in_progress, count completed+sent

**⚠️ חובה:** organization_id על כל query!

### שלב 3 — בנה את המסך

Pixel-perfect מהמוקאפ. כללים:

- רקע: cr50 (#FEFDFB)
- Skeleton loading בזמן fetch
- Stats Strip: go500 לטיוטות, g500 להושלמו
- ReportRow: dot כתום = draft/in_progress, dot ירוק = completed/sent

---

## ✅ Checklist אימות לפני סיום

- [ ] `npm run typecheck` עובר
- [ ] RTL + Rubik
- [ ] cr50 רקע
- [ ] Skeleton בזמן loading
- [ ] organization_id על כל query
- [ ] Stats מחושבות נכון (draft+in_progress, completed+sent)
- [ ] לחיצה על דוח → navigate לReport Detail
- [ ] לחיצה "עוד" בדוחות → Reports List
- [ ] לחיצה "עוד" בפרויקטים → Projects List

---

## 🔴 Iron Rules

- `organization_id` על כל query — חובה מוחלטת

---

## ⏭️ הTask הבא אחרי זה

`TASK_002 — Reports List — מימוש מלא`
```

---

_OUTPUT_TEMPLATE.md — inField PM Agent_
